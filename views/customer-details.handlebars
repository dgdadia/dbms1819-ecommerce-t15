<div class="container">
		<table class="table">
				  <thead class="thead-dark"> </thead>

  <thead class="thead-dark"> </thead>
 
<div style="font: san-serif; font-size: 20px;"> 
<h4 style="color: black;">CUSTOMER DETAILS</h4>


							<p>First Name: {{result.0.first_name}}</p>
							<p>Middle Name: {{result.0.middle_name}}</p>
							<p>Last Name: {{result.0.last_name}}</p>
							<p>Email Address: {{result.0.email}}</p>
							<p>State: {{result.0.state}}</p>
							<p>City: {{result.0.city}}</p>
							<p>Street: {{result.0.street}}</p>
							<p>Zipcode: {{result.0.zipcode}}</p>

</div>

		<table class="table">
			<h4 style="color: black;">ORDER DETAILS:</h4>
			  <thead class="thead-dark"> </thead>
						<tr>
							<th>Ordered Products</th>
							<th>Quantity</th>
							<th>Order Date</th>
						</tr>
					{{#each result}}
					<tbody>
						<tr>
							<td>{{this.product_name}}</td>
							<td>{{this.quantity}}</td>
							<td>{{this.purchase_date}}</td>						
						</tr>
					</tbody>
					{{/each}}
				</table>
			</div>
</div>
</div>
</div>